# V01-09 运维真实 Chromium E2E 报告

> 状态：通过
> 日期：2026-08-11
> 执行角色：运维工程师
> 验收角色：总设计师 / 总架构师

## 1. 核心结论

V01-09 AC-07 已在项目负责人授权的项目内隔离环境完成。`npm run test:e2e` 退出 0，Chromium 3 条真实用例全部通过：

1. 首次登录用户完成强制改密后进入工作台；
2. 无内容维护权限的用户直接访问 `/site` 被路由守卫拒绝；
3. 内容维护用户完成真实 WebP 上传、草稿保存、预览、发布、匿名读取、草稿隔离和撤回。

## 2. 运行环境与隔离边界

- Java：Oracle JDK 25.0.4；后端构建使用 `backend/mvnw`；
- 启动入口：常驻 PTY 承载项目 `./scripts/dev.sh start`；
- 服务：后端 PID 90532、前端 PID 90535；运行时 8080 health 为 `UP`、5173 HTTP 为 200；
- 数据库：仅 `backend/data/internal-admin.db`，由项目 Liquibase 正常入口迁移；
- 上传目录：仅 `backend/data/uploads`，由真实上传按需创建；
- 凭据：只通过 `E2E_*` 环境变量注入，未写入仓库、报告或聊天记录；
- 禁止项：未访问外部数据库、未执行手工 SQL、未运行 `clean`、未清理隔离数据库或上传证据。

## 3. 最终证据

最终运行日志：`logs/v01-09-playwright-fifth-run.log`。

```text
Running 3 tests using 1 worker
3 passed (3.7s)
```

Playwright 结果文件：`frontend/test-results/.last-run.json`。

```json
{
  "status": "passed",
  "failedTests": []
}
```

真实浏览器终态确认：撤回后 `/public` 显示“页面暂不可用”。测试结束后仅终止本轮归属的 PID 90532、90535，8080、5173 均无监听，常驻 PTY 已结束。

## 4. 失败历史与纠正

前四轮均在首个明确失败处停止，没有重复同一路径或降低断言：

1. “新密码”定位器同时匹配“新密码”和“确认新密码”，改为精确标签匹配；
2. restricted/editor 测试账号尚处首次改密状态，通过产品正常认证与改密接口完成测试前置，不使用 SQL；
3. `signIn()` 点击登录后未等待路由稳定，增加离开 `/login` 的确定等待；
4. Element Plus 对话框关闭按钮实际可访问名称为 `Close this dialog`，定位器限定到“草稿预览”对话框并精确匹配。

第五轮从头重跑 3 条用例全部通过。上述过程证明 `playwright test --list` 只能证明用例被发现，不能替代真实浏览器对定位器、账号状态和异步导航的验证。

## 5. 相邻缺陷处置

运行日志中的 `/api/files/` 尾随斜杠异常经总设计师核对，根因是编辑页无主图时预览组件仍渲染空 ID 图片。研发以非空主图条件渲染进行最小修复，并新增回归测试；总设计师独立复验目标测试 3/3、全量 Vitest 8/8、类型检查和构建均通过。

## 6. 最终判定

AC-07 通过，隔离范围、真实浏览器结果和服务收尾证据完整。结合当前实现闭环与空主图回归，V01-09 可以标记为完成。
