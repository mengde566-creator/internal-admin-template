# V01-12A 开发脚本正常关闭等待窗口修复

> 状态：完成（总设计师 / 总架构师验收通过）
> 所属版本：0.1
> 主责角色：研发工程师（甲）
> 设计与派发：总设计师 / 总架构师
> 最终验收：总设计师 / 总架构师
> 创建日期：2026-08-12
> 执行对话：个人项目-普通研发甲；`threadId=019fe584-0c42-70f3-8919-b7a5c56c2885`

## 1. 核心目标

修复 `scripts/dev.sh stop` 对正常 Spring graceful shutdown 的等待窗口过短问题：固定 SHA `ec98d28` 的真实发布验证中，脚本只等待约 2 秒，后端在约 2.02 秒完成正常关闭，导致服务已安全退出但命令返回失败并遗留 PID 文件。

## 2. 权威证据

- 首次 `dev.sh stop` 已发送 TERM，前端正常停止；后端输出“未在限时内退出，保留 PID 记录”；
- `backend.log`：07:04:36.503 开始 graceful shutdown，07:04:38.512 Tomcat 完成，07:04:38.526 Hikari 完成；
- 总设计师只读复核时 PID 9163 已不存在、8080 无监听；
- 现有 `stop_service` 使用 10 次 × 0.2 秒轮询，即约 2 秒；
- 失败保护语义正确：超时后不得强杀，必须保留 PID 文件。修复不得改变该安全边界。

## 3. 最小修改范围

仅允许修改：

- `scripts/dev.sh`：只调整 TERM 后的有界等待窗口到 10 秒，并补充一行解释真实 graceful shutdown 需要余量的注释；
- `docs/team/tasks/evidence/V01-12A_IMPLEMENTATION_REPORT.md`。

禁止改 start/status、PID/命令/cwd/端口归属判定、kill 信号、强杀策略、数据库、代码、测试、CI、其他脚本或任务状态；禁止启动/停止服务、数据库、Maven/npm、E2E、`clean`、提交或推送。

## 4. 完成标准

1. TERM 后最多等待 10 秒，进程提前退出立即停止等待；10 秒后仍存活继续返回非零并保留 PID 文件；
2. 不增加 `kill -9`、宽泛 kill、静默成功或删除未确认 PID 文件的路径；
3. `bash -n scripts/dev.sh`、定向静态扫描、`git diff --check` 通过；
4. 差异只包含等待次数/注释和实施报告，所有其他逻辑字节级保持；
5. 真实 start/status/stop 回归由运维在新 SHA 干净 worktree 从头执行，本任务不得自行冒充运行通过；
6. 完成独立自审后发送 `[V01-12A][申请验收]`，不自行标记完成。
