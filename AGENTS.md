# Internal Admin Template 开发规范

> 版本：0.6
> 状态：生效
> 更新日期：2026-08-12

本规范同时约束人工开发和 AI 生成代码。任何代码修改开始前都必须完整阅读本文件，并在实现、测试和交付时遵守这里的约束。

本规范中的关键词：

- **必须**：不可省略的要求；不满足时不得认为任务完成。
- **禁止**：明确不允许采用的实现。
- **应该**：默认应当遵守；偏离时必须有具体理由。
- **可以**：在符合当前需求时允许使用，但不要求提前建设。

用户在当前任务中给出的明确要求优先于本规范。遇到要求冲突、重大歧义或需要改变既定技术方向时，必须先说明影响，不能自行制造兼容层或兜底路径。

## 0. 沟通与回复格式

- 所有面向用户的回复，开头或第一部分必须先给出足够简洁、明确的核心结论，让用户一眼看出本次要表达的结果、判断或建议。
- 核心结论之后再按需要补充依据、影响、风险、方案和执行细节；禁止把结论埋在长篇过程说明之后。
- 简单问题只给必要答案；复杂问题采用“核心结论 → 关键依据 → 详细补充”的顺序，避免用户必须通读全文才能理解重点。
- 执行型任务优先说明结果与当前状态；尚未完成时优先说明正在解决什么、是否存在阻塞以及下一步。

## 0.1 团队角色加载

- 用户明确指定或切换团队角色时，必须先读取 `docs/team/ROLE_CATALOG.md` 和对应的独立角色文档，再以该角色开展工作。
- 新建或修改团队角色时，必须遵守 `docs/team/ROLE_DEFINITION_STANDARD.md`；一个角色一份文档，只建立当前真实需要的角色。
- 角色名称不能扩大当前任务授权。角色职责与用户要求、已确认需求或本规范冲突时，按更高优先级约束执行并明确报告冲突。

## 0.2 跨版本任务交付

- 版本工作先读取 `docs/team/VERSION_DELIVERY_PROTOCOL.md` 第 2 节完成 L0/L1/L2 分级；只有 L2 需要完整读取协议、执行全部 Gate 并使用 `docs/team/templates/VERSION_ENGINEERING_TASK_TEMPLATE.md`。
- L0 由当前角色直接完成，不派发、不创建任务或证据文档、不要求独立验收；L1 默认由一个主责角色端到端完成，只保留必要验证和一次差异复核；禁止为简单任务机械生成任务书、实施报告和验收报告。
- 只有 L2 或确实涉及多角色文件所有权时才使用跨对话接收、阻塞和验收申请；本可由一个角色安全完成的实现、测试、启动和局部修正不得按角色标签强制拆开。
- 完成依赖被禁止的操作、当前不存在的权限或无法复现的验证时，必须停止并重新设计或拆分任务，禁止先实现后通过多轮返工补救。
- 单次任务记录保存本次证据；可复用教训必须回写交付协议、任务模板、角色规范或自动化门禁，不能只留在对话、验收记录或个人记忆中。
- 任务达到第三次失败、验证轮次超过预计两倍，或治理/证据文件开始多于产品与测试变更时，必须触发成本止损复核；可恢复的命令、工作目录、定位器和临时脚本错误不得升级为新的版本任务。
- 多对话协作只在并行确实降低总成本且文件、端口、进程和事实源能够隔离时启用；否则保持单角色闭环。

## 1. 核心原则

### 1.1 只实现明确需求

- 必须只实现当前任务和验收条件需要的能力。
- 禁止为“以后可能需要”提前增加微服务、插件系统、通用工作流、兼容层或扩展点。
- 禁止创建没有真实调用者的接口、配置项、工具类和抽象基类。
- 禁止在当前任务中顺手重构、格式化或移动无关代码。
- 不确定是否需要抽象时，必须先保留清晰的具体实现。

### 1.2 保持最低必要复杂度

- 每增加一个模块、架构层、运行时依赖、设计模式或通用组件，都必须能说明它解决的当前问题。
- 只有一个实现且没有明确替换需求时，不应该增加接口或工厂。
- 只有一个使用场景时，不应该为了形式统一创建通用框架。
- 相似代码只有在语义稳定并形成真实重复后才提取；不能仅因代码外形相似就抽象。
- 优先使用语言、Spring、MyBatis-Plus、Vue 和项目现有依赖已经提供的能力。

### 1.3 一个主路径，失败必须可见

- 每个功能必须有一条明确的正常执行路径和明确的失败行为。
- 禁止用默认值、空对象、空集合或备用实现掩盖配置错误、数据库错误和程序缺陷。
- 空集合只能表示查询成功但没有数据，不能表示查询执行失败。
- 数据不存在、业务拒绝、参数错误和系统异常必须能够区分。
- 可选能力可以降级，但必须是明确需求，并且调用方能够知道当前处于降级状态。

### 1.4 严格控制变更范围

控制范围是本项目的最高优先级规则之一。简单需求必须保持简单，禁止将一次局部修改扩大成架构调整、通用化改造或全项目重构。

- 修改前必须明确本次任务要解决的问题、必须触及的范围和完成条件。
- 实现必须采用能够完整解决问题的最小连贯改动。最小改动不等于强行只改一个文件；数据库字段变更所必需的变更集、DO、DTO、Mapper、接口、前端和测试属于同一连贯范围。
- 除上述必要传播外，禁止修改与当前需求无直接关系的模块、公共契约、目录结构、命名、依赖和配置。
- 发现相邻缺陷、旧代码或可优化点时，应该单独报告；除非它直接阻塞当前任务，否则禁止顺手修复。
- 禁止把功能修改与大范围重命名、代码搬迁、格式化、依赖升级或抽象提取混在同一次改动中。
- 禁止因为修改一个页面而重写整个前端布局、请求层或状态管理。
- 禁止因为修改一个字段而重新设计整张表、整个 DTO 体系或通用字段框架。
- 禁止因为修复一个 Mapper 查询而替换数据访问框架、重写无关 SQL 或增加通用 Repository 层。
- 禁止为了兼容未提出的旧版本、输入格式、数据库行为或调用方式增加额外分支。
- 如果完成需求确实需要扩大范围，必须先说明扩大原因、影响文件和替代方案，得到明确确认后才能继续。
- 完成后必须逐项检查差异；任何不能直接对应当前需求或必要验证的修改都应该删除或拆到独立任务。

判断一项修改是否应该保留时，必须能够回答：

1. 它对应当前需求中的哪一项？
2. 如果删除它，当前需求或必要测试会在哪里失败？
3. 它是否引入了新的维护成本、行为分支或公共契约？

前两项无法明确回答，或者第三项为“是”但没有得到确认时，该修改不得保留。

## 2. AI 开发流程

### 2.1 按风险读取上下文

开始任何分析或修改前，AI 必须按任务范围读取上下文，不能只根据文件名或历史对话猜测要求。

所有任务必须读取离目标文件最近的 `AGENTS.md`。其余上下文按交付等级加载：

- **L0**：只读取目标文件、直接相关测试/配置和最近的规范；纯措辞、索引、测试定位器、命令参数等任务，不强制读取项目 README、需求索引、完整工程约定和历史材料。
- **L1**：读取根 `AGENTS.md`、`README.md`、`requirements/README.md`、直接关联需求、模块代码和测试；代码实现再读取 `docs/development/ENGINEERING_CONVENTIONS.md` 的相关章节与提交前清单。
- **L2**：完整读取根规范、项目与需求索引、直接关联需求、相关模块规范/能力包、工程约定以及任务书列出的权威材料。

任务存在对应需求时，必须继续读取 `requirements/README.md` 索引中关联的需求文档。只有状态为“已确认”的要求可以直接作为实现依据；“草稿”和“待确认”内容只能用于讨论，不能擅自实现。

L1/L2 后端任务继续读取 `backend/AGENTS.md`、相关模块 README 和相关需求文档；L1/L2 前端任务继续读取 `frontend/AGENTS.md`、相关模块 README 和相关需求文档。对应目录或文件尚未创建时，不得伪造其内容，也不需要为了满足读取列表提前创建空文件。

涉及数据库结构时，必须额外读取相关数据库设计、现有 Liquibase 变更集、DO 和 Mapper。涉及参考材料时，仅在需求文档明确关联或用户明确指定后读取 `references/` 中的对应材料，禁止把材料自动解释为正式需求。

L0 代码修正只读取与改动直接相关的代码约定；L1/L2 代码实现必须读取 [docs/development/ENGINEERING_CONVENTIONS.md](../../docs/development/ENGINEERING_CONVENTIONS.md) 的相关章节和“提交前自查清单”。禁止为机械小改加载整套无关材料。

开始工作时，L0 只需用一句话声明等级、范围和验证；L1/L2 再简短声明：

- 本次实际读取的规范、要求和材料；
- 本次准备处理的范围；
- 本次未加载的无关材料不参与决策。

如果工作过程中发现需要读取新的要求或扩大范围，必须先说明原因再继续。

### 2.2 执行顺序

AI 执行代码任务时按风险选择必要步骤。L0 只执行范围确认、最小改动、差异检查和一项最近验证；L1/L2 再按下列顺序展开，禁止让 L0 机械走完十步：

0. **任务分型与机制加载**（机制按任务类型加载，小任务不背大机制——守住"有方法的轻"）：

   | 任务类型 | 判定 | 必须加载的机制 |
   | --- | --- | --- |
   | 新接口/新功能 | 涉及新的 API、页面或能力 | **完整契约清单**（INTERFACE_CONTRACT，含材料驱动推导）+ **交付前用户视角走查**（自查 3.2）+ 回归测试 |
   | UI/视觉变更 | 布局、样式、交互调整 | 需求漏斗（视觉类必须先给设计意图并引导确认，红线 16.2 第 6 条）+ 走查 |
   | Bug 修复 | 现有功能不符合预期 | **回归测试**（复现问题的用例）+ 轻量走查（自查 3.2 六问） |
   | 纯内部改动 | 重构、依赖、配置、文档 | 与风险最近的静态检查或定向测试；只有影响运行时代码时才编译 |

   分型不清时按较重一档执行；契约清单与走查的产物随提交保存（清单在 `docs/system/api/<模块>/`，走查结论写入提交说明），供用户或后续 AI 抽查。

1. 阅读本规范、相关模块代码、现有测试和配置。
2. 搜索项目中是否已经存在相同或相近能力。
3. L1/L2 涉及非平凡设计、新功能、视觉、架构选择、新依赖或陌生 API 时，先检索官方文档和成熟实现作为学习输入。L0 机械调整、原因明确的局部缺陷和完全受既有契约约束的工作不联网；用户明确要求检索时除外。
4. **材料优先判断**：涉及字段校验、错误提示、业务规则判断时，先查已有材料（表结构/需求/现有代码）——**材料能推导的不问用户、不猜**（表结构 NOT NULL→必填、VARCHAR(n)→长度，直接照抄）；确需用户决策的只问"差值"，用选择题问。材料与代码冲突时报告，不闷头按旧材料干（机制总纲见 WORKING_MODEL 12.6，模板见 INTERFACE_CONTRACT）。
5. 明确本次修改范围、假设、预计触及的文件类型和可验证的完成条件。
6. 采用满足需求的最小连贯改动，不修改无关文件。
7. 如果实现过程中发现必须扩大范围，停止修改并先说明原因和影响。
8. 实现后检查完整差异，主动查找重复、死代码、错误兜底和越界改动。
9. 执行与修改风险相匹配的编译、静态检查和测试。
10. 如实说明实际执行的验证；禁止声称未执行的测试已经通过。

AI 禁止生成或保留：

- 暂时返回空数据、固定成功或假结果的占位实现；
- 没有当前用途的 `TODO`、空方法和兼容分支；
- 捕获所有异常后继续执行的“安全网”；
- 没有调用方的工具类、扩展接口或配置开关；
- 重复的日期、JSON、分页、响应或对象转换工具；
- 为展示设计模式而增加的层次和类；
- 注释掉的旧代码和无法到达的分支；
- 通过跳过测试、删除断言或降低校验让任务表面通过的修改。

出现以下情况时必须停止并说明，不能自行扩大实现：

- 需求存在会改变数据模型、权限模型或模块边界的歧义；
- 需要替换已确定的主要技术；
- 需要引入与现有能力重叠的大型运行时依赖；
- 需要执行破坏性数据迁移或兼容不明确的数据库操作；
- 发现现有用户代码与任务目标直接冲突。

### 2.2.1 有界等待、阻塞升级与人工介入

- 网络访问、依赖解析、下载安装、编译、测试、应用启动、浏览器操作、外部服务和工具调用都必须设置有限等待预算、可观察的进度信号和明确停止条件，禁止无界等待或反复轮询同一状态。
- 连续 60 秒没有新输出、产物变化、进程状态变化或其他可证明的进展时，先停止盲目等待并诊断；可恢复的工作目录、参数、测试代码或临时脚本错误由当前执行者直接最小修正一次。只有风险阻塞、同一实质路径再次失败或修正需要扩大授权时才标记阻塞。
- 停止盲等后不能只等待环境自行恢复。必须按任务边界主动评估可立即验证的安全替代路径，例如现有本地依赖或缓存、单次命令指定的可信镜像、已安装工具、等价验证方式或由用户执行的最小手工步骤；替代路径同样必须有短时限和完整证据。改变锁定版本、永久 registry 配置、主要工具或既定验收标准仍须先取得确认，禁止为了绕过阻塞擅自降级或换技术。
- 合理预计会超过 60 秒且能够证明持续有真实进展的操作可以继续，但必须至少每 60 秒向用户给出一次简短进度更新，并始终保留硬停止条件；不得让用户在不知道当前状态的情况下长期等待。
- 如果风险阻塞位于关键执行链路，导致依赖无法获得、项目无法编译、应用无法启动、页面效果无法查看、必要测试无法运行或最终验收无法继续，必须立即向项目负责人报告。可以在原范围内安全修正的普通执行错误不请求人工介入。
- 阻塞报告必须包含：当前结论、失败命令或证据、已安全尝试的处理、对后续步骤的直接影响、需要人工完成的精确动作，以及不依赖该阻塞仍可继续的工作。
- 常见人工介入包括网络或代理调整、登录或凭据提供、权限放行、运行环境安装、外部服务恢复和用户手工执行指定步骤；未经授权不得以人工介入为名扩大权限或执行破坏性操作。
- 一个工作流被阻塞时，与其不存在文件、端口、进程或事实源冲突的独立任务应该继续推进；禁止因单点阻塞让整个团队无理由停摆。

### 2.3 AI 装配规则

项目愿景见 [docs/PROJECT_VISION.md](docs/PROJECT_VISION.md)：AI 是装配工，人定架构；AI 必须在既定约束内装配，禁止自行做架构决策。装配规则是防幻觉与不一致的第一道闸门，必须遵守：

**素材引用**

- AI 只能引用素材库 `02-已批准` 中审核状态为"已批准"的素材（组件、设计令牌、动效配方），禁止为图方便自行现编视觉组件、配色、圆角、阴影或动效参数替代已批准素材；
- 视觉样式的唯一来源是已批准的 `tokens.css` 语义令牌，禁止并行创建第二套全局 CSS 变量或主题切换机制；
- 引用已批准素材时必须随附并保留其许可证义务（MIT 版权声明与许可原文），禁止剥离署名；
- 素材库中"待审核""已驳回""已停用"的素材禁止进入项目；GSAP 相关代码只能按"项目内固定版本依赖"评估，不得作为素材引用。

**依赖与版本**

- 依赖版本必须使用项目锁定清单（BOM/统一版本管理），禁止 AI 自造版本号或"取最新版"；
- 引入新运行时依赖必须说明解决的当前问题、现有能力为何不足、许可与维护状况，且先提出再实施；未经确认禁止添加。

**模块能力包**

- 开发或新增业务模块前，必须加载该模块的 AI 提示词与设计文档（`backend/modules/<module>/capability/AI_PROMPT.md` 与 `CONTRACT.md`）；模块实现必须与能力包清单一致（提示词、设计、数据契约、实现资产、测试、素材引用六项齐全）；
- 新模块必须复制能力包模板结构，禁止自由发挥目录、命名或分层；模块可单独引入、可组合，组合必须遵守依赖方向（`app-server → 业务模块 → 基础模块`）和跨模块公开 API/本地事件契约；
- 禁止在没有能力包模板的情况下以"新模块"名义创建空目录、占位 Service 或未注册路由。

**一致性**

- 同一概念全项目只使用一个命名与模型；DO/DTO、权限模型、错误语义不得在模块间产生第二套平行定义；
- 装配完成后，按能力包清单与质量门禁逐项核对；任何一项缺失或未验证，不得声明完成。

## 3. 总体架构

项目采用模块化单体和前后端分离架构。第一阶段不拆微服务。

后端以业务模块组织代码，模块内部采用务实分层：

```text
Controller
    ↓
Service
    ↓
Mapper extends BaseMapper<DO>
    ↓
Database
```

职责要求：

- Controller 负责 HTTP 协议、参数校验、身份入口和调用 Service。
- Service 负责业务规则、权限协作、事务和用例编排。
- Mapper 负责数据库访问。
- Mapper XML 负责复杂查询、联表查询和数据库专属 SQL。
- 禁止 Controller 直接调用 Mapper。
- 禁止跨模块直接调用对方的内部 Mapper。
- 禁止为 MyBatis-Plus Mapper 再创建无实际业务价值的 Repository/RepositoryImpl。
- 禁止创建通用 BaseController、BaseService 或第二套 BaseMapper 框架。
- 禁止产生模块循环依赖。

## 4. Service 规范

Service 默认使用具体类：

```java
@Service
public class UserService {
    // use cases
}
```

- 禁止为每个 Service 机械创建 `Service + ServiceImpl` 组合。
- 禁止仅为形式统一创建空壳接口。
- 默认不使用 `IUserService` 一类接口命名。
- 默认不要求继承 MyBatis-Plus `IService` 或 `ServiceImpl`。
- Controller 注入具体 Service 类。

只有满足以下至少一种情况时才可以建立 Service 接口：

- 已经存在多个真实实现；
- 需要隔离短信、邮件、对象存储、AI 提供商等外部能力；
- 模块需要向其他模块暴露稳定契约；
- 实现确实需要被部署配置或插件替换。

## 5. 数据对象规范

项目只使用 DO 和 DTO 两类数据对象，不引入 VO、BO、PO 等同义模型。

### 5.1 DO

- DO 使用 `DO` 后缀，例如 `UserDO`。
- 一个 DO 默认对应一张主表。
- DO 只包含数据库字段、字段说明和必要的 MyBatis-Plus 映射注解。
- DO 不直接作为 Controller 的请求参数或响应结果。
- DO 不包含页面展示逻辑和 HTTP 协议逻辑。
- DO 中的密码哈希、密钥、内部状态和审计字段不得因对象复用而暴露给前端。

### 5.2 DTO

DTO 由具体功能按需创建，例如：

```text
CreateUserDTO
UpdateUserDTO
UserQueryDTO
UserListDTO
UserDetailDTO
```

- 创建、修改、查询、列表和详情在字段语义不同时必须使用不同 DTO。
- 禁止创建同时承担所有请求和响应场景的万能 DTO。
- DTO 可以包含 Jakarta Validation 校验注解。
- DTO 不包含数据库写入、查询或事务逻辑。
- 复杂联表查询可以由 Mapper 直接映射为对应 DTO，不需要额外创建 BO 或 VO。
- DTO、MyBatis-Plus 分页对象和 Wrapper 不得无边界地跨层传播。

## 6. DO 与 DTO 转换

字段名称、类型和语义完全一致时，可以使用 Spring `BeanUtils.copyProperties` 做浅复制：

```java
UserDetailDTO target = new UserDetailDTO();
BeanUtils.copyProperties(source, target);
```

以下情况必须显式转换或赋值：

- 字段名称或类型不同；
- ID、时间、枚举等需要格式转换；
- 状态码需要转换为展示内容；
- 数据来自多张表或包含嵌套结构；
- 涉及密码、密钥、权限等敏感字段；
- 涉及 ID、创建人、更新时间、逻辑删除等系统维护字段；
- 更新 DTO 中的 `null` 表示“不修改”；
- 同名字段的业务语义不同。

转换规则：

- 转换代码由当前功能自行维护，少量转换直接写清楚。
- 相同转换形成真实重复后，可以在当前模块内提取简单方法。
- 暂不引入 MapStruct。
- 禁止同时引入多个 Bean 转换工具。
- 禁止使用 `Map<String, Object>` 作为对象转换中介。
- 禁止创建自动递归处理所有对象的万能转换框架。
- 禁止只为改名而包装 `BeanUtils.copyProperties`。
- 禁止转换失败后返回空 DTO 或部分初始化对象。
- 禁止将 UpdateDTO 无差别复制到 DO 后直接更新。

## 7. MyBatis-Plus 与 SQL

### 7.1 使用边界

- 简单单表 CRUD 使用 MyBatis-Plus `BaseMapper`。
- 简单条件查询使用类型安全的 Lambda Wrapper。
- 联表查询、权限过滤、报表和性能敏感查询使用 Mapper XML。
- 禁止为每个 `BaseMapper` 方法再创建行为完全相同的 Mapper 方法。
- Mapper 写操作主要使用 DO；查询条件和查询投影可以使用 DTO。
- 禁止在 Controller 中构造或接收 Wrapper。

### 7.2 查询与性能

- 禁止在循环中逐条查询可一次获得的数据，避免 N+1 查询。
- 批量场景必须优先使用批量查询或批量写入。
- 列表接口默认分页，并设置合理的最大页容量。
- 禁止无条件读取可能持续增长的整张表。
- 查询应该只选择实际需要的字段；禁止无理由使用 `SELECT *`。
- 排序字段必须使用服务端白名单，禁止直接拼接前端排序字段。
- 禁止将前端输入直接传入 Wrapper 的 `apply`、`last` 等原生 SQL 片段。
- 索引必须服务于真实的查询、关联、唯一约束和排序需求，禁止为每个字段机械创建索引。
- 新增或调整重要查询时，必须检查查询次数、执行计划或对应索引是否合理。

### 7.3 事务

- 事务边界放在 Service 层。
- 只读查询不应无理由开启写事务。
- 禁止在事务中执行无界远程调用或长时间阻塞操作。
- 批量写入必须考虑事务大小和失败后的数据一致性。
- 禁止通过捕获异常并不再抛出导致事务错误提交。

## 8. 数据库与迁移

### 8.0 数据库执行边界

数据库安全边界按“目标环境与执行路径”判断，不把项目正常运行产生的数据库写入等同于绕过项目直接改库。

- 项目负责人明确授权后，可以通过项目已经定义的正常入口启动应用、执行 Liquibase、运行自动化测试和端到端测试，并写入位于本项目工作区内、与非测试数据隔离的本地开发或测试数据库；
- 上述授权只适用于项目代码、项目脚本和项目锁定工具链管理的正常执行路径，执行前必须明确数据库文件、上传目录、端口和预期副作用，执行后如实报告结果；
- 禁止绕过项目入口手工执行 DDL/DML、直接编辑数据库文件、伪造迁移状态或用临时 SQL 修补数据；
- 禁止把本地开发或测试授权扩大到外部数据库、共享数据库、生产数据库或来源不明的数据文件；外部数据库默认只读，任何写入必须取得针对目标环境和具体操作的单独明确授权；
- 自动化测试必须使用可识别、可隔离、可清理的测试数据，不得静默复用个人开发数据或以回退数据库掩盖配置错误；
- 当前运行环境存在更高优先级数据库限制时，必须遵守更高优先级限制并明确报告，禁止通过修改本文件绕过运行环境约束。

项目支持以下数据库方向：

```text
SQLite
MySQL
PostgreSQL
Oracle
```

### 8.1 数据库选择

- 完全没有数据库配置时，使用项目本地 SQLite 文件数据库。
- 用户提供外部数据库配置后，必须使用指定的外部数据库。
- 外部数据库配置不完整、连接失败或迁移失败时，应用必须启动失败并给出明确原因。
- 禁止外部数据库失败后静默回退到 SQLite。
- 切换数据库不等于自动迁移业务数据；数据迁移必须是显式操作。

### 8.2 多数据库 SQL

- 通用 SQL 应该使用目标数据库共同支持的语法。
- 确实存在数据库差异时，使用 MyBatis `databaseIdProvider` 提供专属 SQL。
- 数据库专属 SQL 必须清楚标记适用数据库，并提供必要的其他数据库实现。
- 禁止把 MySQL 枚举、Oracle 序列、PostgreSQL 数组等专属能力放入通用核心，除非已有明确需求和兼容设计。

### 8.3 结构变更

- Liquibase 是数据库结构和基础数据版本变更的唯一入口。
- 禁止依赖 ORM 或启动代码自动修改表结构。
- 禁止手工修改数据库后不补充变更集。
- 变更集必须可重复验证，并考虑 SQLite、MySQL、PostgreSQL 和 Oracle 的差异。
- 生产基础数据与演示数据必须分离。

### 8.4 Bug 修复中的表结构保护

修复 Bug 默认不授权修改数据库结构。现有表、列、类型、长度、默认值、非空约束、唯一约束、外键和索引都属于受保护的持久化契约，不能为了让代码更容易修改而顺手调整。

- 排查 Bug 时必须先确认根因在代码、数据、配置还是表结构，禁止看到映射不便就直接改表。
- 能在不破坏正确数据语义的前提下通过代码修复的问题，禁止修改表结构。
- 禁止为了绕过空值、类型转换、查询条件或映射错误而放宽非空约束、扩大字段、删除约束或新增冗余列。
- 禁止将 Bug 修复夹带成表结构重命名、字段统一、索引整理或历史数据重构。
- 禁止修改已经执行或发布过的 Liquibase 变更集；必须新增后续变更集。
- 禁止通过启动代码检测缺列后自动建列、捕获“列不存在”后走备用 SQL，或在不同环境维护未版本化的表结构。

如果确认 Bug 无法在正确语义下仅通过代码修复，必须先提出数据库变更说明，至少包含：

1. Bug 根因以及为什么代码修复不足以解决；
2. 拟修改的表、列、索引或约束；
3. 对现有数据和四种目标数据库的影响；
4. 数据迁移、兼容和回滚方案；
5. 需要联动修改的 DO、DTO、Mapper、接口、前端和测试范围。

未得到明确确认前，禁止实施数据库结构修改和依赖该结构的新代码。确认后的实施顺序必须是：

1. 先确定最终表结构和数据迁移方案；
2. 新增 Liquibase 变更集；
3. 验证从现有版本升级，确认历史数据不会丢失或被错误转换；
4. 再修改 DO、Mapper、Service、接口和前端；
5. 最后执行迁移测试、回归测试和目标数据库兼容性验证。

涉及删除列、缩短字段、修改类型、增加非空约束等破坏性变更时，应该采用“先扩展、迁移数据、切换代码、最后清理”的分阶段方式。除非用户明确要求并确认数据影响，否则禁止在一次 Bug 修复中直接删除或不可逆修改现有数据结构。

## 9. 异常、重试与降级

### 9.1 异常处理

- 配置错误和基础设施初始化错误必须快速失败。
- 业务拒绝、数据不存在、参数错误和系统错误必须使用不同语义表达。
- 异常应在合适的边界转换，禁止每层捕获后重新包装成无意义的新异常。
- 同一个异常不应在 Controller、Service、Mapper 多层重复记录。
- 禁止 `catch (Exception)` 后返回默认值、空对象或空集合。
- 禁止捕获异常后仅记录日志并继续执行本应失败的业务。
- 禁止向前端暴露堆栈、SQL、密钥和内部实现信息。

### 9.2 重试

- 重试只适用于明确的瞬时故障。
- 被重试操作必须具备幂等性，或具有明确的重复执行保护。
- 重试必须有最大次数、间隔和最终失败行为。
- 禁止无限重试、递归重试和多层同时重试。
- 数据校验失败、权限拒绝、配置错误和确定性的业务失败禁止重试。

### 9.3 降级和默认值

- 降级必须来自明确需求，不能由 AI 自行添加。
- 降级后必须保留可观察状态，不能让调用方误以为主流程成功。
- 必填配置缺失时禁止猜测默认值。
- 禁止解析失败后连续尝试多个未约定格式。
- 可选配置可以有文档化默认值，但默认值必须唯一且行为明确。

## 10. Vue 前端

前端采用 Vue 3、TypeScript 严格模式和 Vite。第一版使用客户端渲染 SPA，不引入 Nuxt 或 SSR。

### 10.1 代码组织

- 使用 Composition API 和 `<script setup lang="ts">`。
- 禁止无理由使用 `any`、类型断言和 `@ts-ignore`。
- 页面负责用例组合，业务组件负责可复用交互，基础组件负责稳定的通用展示。
- 业务组件只有在出现真实重复后才提取。
- 禁止无意义地二次包装 UI 组件库的每一个基础组件。
- 路由页面使用动态导入，避免首屏加载全部管理模块。

### 10.2 状态管理

- Pinia 只管理会话、权限、内部界面配色偏好和确需跨页面共享的客户端状态；公开主页配色属于草稿与发布数据，不得以 Pinia 或 `localStorage` 作为权威来源。
- TanStack Query 管理接口列表、详情、分页结果、缓存、请求去重和失效。
- 禁止在 Pinia、页面组件和查询缓存中重复保存同一份服务端数据。
- Query Key 必须按业务模块统一定义，禁止页面随意拼接没有约定的 Key。
- 新增、修改和删除成功后必须精确失效相关查询。
- 用户退出时必须清理用户相关的状态和查询缓存。
- 分页、排序和筛选等可恢复页面状态应该保存在 URL Query 中。

### 10.3 表格和请求

- 表格默认使用后端分页、筛选和排序，禁止默认一次加载全部数据。
- 搜索输入应该防抖，筛选条件变化时应该取消已经无意义的旧请求。
- 字典、配置和组织树等低频数据可以缓存，但必须有明确失效策略。
- 大数据表格优先通过后端分页解决；只有明确场景才使用虚拟化。
- 页面必须明确处理加载、空数据、失败和无权限状态。
- 禁止请求失败后静默展示空列表。

### 10.4 前端权限

- 前端权限只负责界面展示和导航体验，Java 后端是权限判定的最终依据。
- 前端路由由代码维护，功能导航依据后端权限结果过滤；0.1 不建设动态菜单。
- 路由 `meta` 可以描述登录要求、页面权限和标题。
- 菜单隐藏和按钮隐藏不能替代后端鉴权。

## 11. API 与安全

- API 请求和响应必须使用明确 DTO，禁止直接暴露 DO。
- 参数必须在进入业务逻辑前完成基础校验。
- 权限必须由后端校验，不能信任前端传入的用户、角色和数据范围。
- 敏感字段默认不查询、不返回、不记录日志。
- 批量、导出和文件接口必须有明确的数据量、类型和权限限制。
- 禁止使用字符串拼接生成 SQL、文件路径和命令。
- 错误响应不得暴露堆栈、数据库连接信息和内部文件路径。

## 12. 测试规范

- 修改必须有与风险相匹配的验证。
- Bug 修复必须增加能够复现问题的回归测试。
- 业务规则应该使用单元测试覆盖。
- 复杂 Mapper SQL 必须使用真实数据库集成测试验证。
- 数据库专属 SQL 必须在适用数据库上验证。
- 登录、权限、越权和关键管理流程需要端到端测试。
- 禁止只 Mock 被测对象自己的核心行为。
- 禁止通过删除断言、跳过测试、扩大容差或吞掉异常使测试表面通过。
- 测试数据必须表达业务场景，不能依赖不稳定的随机结果。

## 13. 依赖与工具

- 一个领域只保留一个主要工具，禁止引入功能重叠的依赖。
- 新依赖必须解决当前真实问题，并说明现有能力为什么不足。
- 禁止为了一个简单工具方法引入大型工具包。
- 禁止复制已有依赖已经可靠提供的通用能力。
- 依赖版本必须统一管理和锁定，禁止在多个模块随意声明不同版本。
- 禁止使用来源不明、停止维护或安全状态不清楚的依赖。
- 暂不引入 MapStruct，也不增加第二套对象复制工具。

## 14. 命名、注释与清理

### 14.1 方法 Javadoc

手工编写的业务方法必须在方法声明正上方提供 Javadoc。注释必须让阅读者在不展开实现的情况下知道方法用途、方法名、按步骤展开的执行链路、输入、输出和可预期异常。

以下方法必须提供完整 Javadoc：

- Controller 接口方法；
- Service 公共方法和模块对外 API；
- 自定义 Mapper 方法；
- 定时任务、事件监听器和异步任务入口；
- 包含业务规则、数据库交互、状态变更或外部调用的私有方法；
- 被其他方法执行链路通过 `{@link}` 引用的方法。

以下内容不要求重复编写无意义 Javadoc：

- 自动生成或简单的 getter、setter、record accessor；
- 能完整继承父类说明的覆写方法，此时使用 `{@inheritDoc}`；
- Lambda 表达式和局部函数；
- 使用清晰 `@DisplayName` 表达场景的测试方法；
- 没有独立业务语义、且不应该被提取成方法的一行代码。此类方法应该优先合并回调用方，而不是用注释合理化过度拆分。

Javadoc 推荐模板：

```java
/**
 * 创建用户。
 *
 * 方法：{@code createUser}
 *
 * 执行链路（共 6 步）：
 * 1. 调用 {@link #ensureUsernameAvailable(String)} 检查用户名是否可用；
 *    用户名已存在时终止创建并抛出业务异常。
 * 2. 创建 {@link UserDO}，调用 {@link BeanUtils#copyProperties(Object, Object)}
 *    复制名称、账号等语义一致的基础字段。
 * 3. 调用 {@link PasswordEncoder#encode(CharSequence)} 编码原始密码，
 *    只将密码哈希写入 {@link UserDO}。
 * 4. 调用 {@link BaseMapper#insert(Object)} 持久化用户，取得应用生成的用户 ID。
 * 5. 调用 {@link #assignDefaultRole(Long)} 建立用户与默认角色的关联；
 *    角色不可用时回滚本次创建事务。
 * 6. 调用 {@link #toDetailDTO(UserDO)} 生成安全的详情 DTO 并返回，
 *    响应中不包含密码哈希和内部审计字段。
 *
 * @param dto 用户创建数据
 * @return 创建后的用户详情
 * @throws BusinessException 用户名已存在或默认角色不可用时抛出
 * @see UserMapper
 */
@Transactional
public UserDetailDTO createUser(UserCreateDTO dto) {
    // implementation
}
```

链接语法：

```java
{@link #sameClassMethod(String)}
{@link OtherClass#otherMethod(Long)}
{@link OtherClass}
@see OtherClass#otherMethod(Long)
```

Javadoc 规则：

- 第一行必须用简洁中文说明业务目的，不能只重复“处理数据”“执行操作”。
- `方法：` 后必须写实际方法名，并使用 `{@code methodName}`。
- `执行链路：` 必须使用普通的 `1.`、`2.`、`3.` 编号按真实顺序列出步骤，并注明总步骤数。
- 禁止使用 `<ol>`、`<li>` 等 HTML 列表标签描述执行链路，优先保证源码中的直接可读性。
- 每一步必须说明执行了什么、关键判断或转换是什么，以及该步骤产生的结果或副作用；禁止只罗列方法名。
- 步骤中涉及方法调用时，必须在对应说明中补充 `{@link}`；禁止先写一份纯文本流程，再在其他位置堆积链接。
- 调用项目内其他方法或类型时必须使用 `{@link}`；作为关键边界的框架或第三方方法也应该链接。
- 同类方法使用 `{@link #method(Type)}`，其他类使用 `{@link Class#method(Type)}`；重载方法必须写参数类型以消除歧义。
- 执行链路应覆盖参数或业务校验、关键数据准备、主要业务规则、持久化、外部副作用和返回结果中实际存在的步骤。
- 执行链路只展开当前方法的一层主要流程；被调用方法的内部细节由其自己的 Javadoc 说明，父方法不得重复复制完整子流程。
- 不调用其他方法的步骤仍然要说明实际操作，但不能为了满足格式虚构链接或无意义步骤。
- 简单方法可以只有一至两个步骤，复杂方法可以有更多步骤；禁止为了凑固定数量拆分或合并真实步骤。
- getter、setter、日志、普通集合遍历和无业务意义的框架调用不单独作为步骤。
- 存在条件分支时，必须在对应步骤中说明触发条件、不同路径和是否提前结束，不能把可选路径写成必然执行。
- 涉及事务时，步骤顺序必须与真实写入及回滚边界一致；涉及异步操作时必须明确同步部分与异步部分的边界。
- `@param` 必须说明业务含义，禁止只把参数名复制一遍。
- 非 `void` 方法必须使用 `@return` 说明返回语义，禁止只写“返回结果”。
- 可预期的业务异常和关键系统异常必须使用 `@throws` 说明触发条件。
- `@see` 用于补充强相关入口或契约，不能堆积无关引用。
- 方法重命名、调用顺序变化、异常条件变化时，必须在同一次修改中同步更新 Javadoc。
- 所有 `{@link}` 必须能够被 IDE 和 Javadoc 工具解析，禁止保留失效链接。
- 禁止复制同一套空泛执行链路到多个方法，也禁止用长篇注释掩盖命名不清或方法职责混乱。

### 14.2 一般命名、注释与清理

- 名称必须表达业务含义，禁止使用 `data1`、`temp`、`handleThing` 等模糊命名。
- 注释解释原因、约束和非显而易见的决策，不重复描述代码表面行为。
- 禁止保留注释掉的旧实现；历史由版本控制保存。
- 禁止提交调试输出、临时开关、演示账号和本地密钥。
- 禁止保留未使用的类、方法、参数、导入和配置。
- 完成修改后必须移除临时日志、测试数据和生成过程中产生的无用文件。

## 15. 完成标准

任务只有同时满足以下条件才可以认为完成：

- 实现满足明确需求，没有额外扩展范围；
- 分层、DO/DTO、Service 和 Mapper 使用符合本规范；
- 没有新增静默兜底、重复查询、无界重试和无意义抽象；
- 数据库变更具有 Liquibase 变更集；
- 前后端权限边界没有被削弱；
- 相关编译、静态检查和测试已实际执行，或明确说明未执行原因；
- 差异中没有死代码、占位实现、无关格式化和敏感信息；
- 新增或修改的业务方法具有与实际实现一致、链接有效的 Javadoc；
- AI 装配类任务遵守 2.3 装配规则：仅引用素材库已批准素材、使用锁定依赖版本、按模块能力包清单交付；
- 文档和配置在行为发生变化时得到同步更新。

## 16. 开发红线（模板级强制规则）

以下红线适用于本模板及所有派生项目，违反即视为重大失误，开发过程中必须遵守：

### 16.1 数据库操作红线

1. **禁止随意删除/重建数据库**：删库是数据事故级别的操作，不是常规调试手段；
2. 启动/运行失败 → **先看堆栈定位代码逻辑 bug**（绝大多数是代码问题，与数据无关）；
3. 确认是数据问题后，**用 Liquibase 变更集修复**（UPDATE 回填、清理脏行），禁止手工改库；
4. **删库/清数据必须同时满足**：数据确认为一次性测试数据（非真实、非用户验证过的数据）+ 用户明确知晓并同意 + 已先修复代码 bug 再验证；
5. **删库/清数据前必须评估关联存储**：上传文件、附件等与数据库分离存储的资产——删库可能只清元数据、留下孤儿文件，或反之。

### 16.2 开发过程红线

1. 新增逻辑前先检查与现有代码是否**功能重叠**（如补权限逻辑 vs 创建流程已有写入），避免重复执行/冲突；
2. 涉及数据库差异（SQLite ALTER 等）、框架版本差异（Spring Boot 4、Jackson 3 等）**先查证再写**，不靠运行失败发现；
3. 写完立即自查（见 ENGINEERING_CONVENTIONS 第 3 节清单），禁止"等会统一修"；
4. 生产代码禁止输出临时日志、测试开关与调试残留；
5. **同一问题被反复提出（≥2 次）时，禁止继续凭自己方向本地修补**——必须停止，上网查找同类成熟项目/设计作为参考，基于参考给出完整方案后再动手；禁止一次改一个点让用户反复验证；
6. 视觉/布局/交互类问题**必须先给出完整设计意图再写代码**——设计意图应**引导用户参与确认**（页面类型、气质、参考方向、参数偏好），**禁止 AI 凭空猜测用户意图代替确认**（如把"铺满"猜成全屏 hero）；禁止边改边看、挤牙膏式修改；
   - **先找参考再问**：涉及视觉/布局，先上网找 2-3 个成熟参考作为"候选池"，再让用户在参考/方案间选择；
   - **多让用户做判断题/选择题，少做问答题**：把模糊需求转化为可选项让用户挑（"更像 A 还是 B？"），用户做低成本确认；禁止把开放式问题（"你想要什么风格？"）抛给用户当问答题。
7. **涉及新功能/UI 变更/表结构的需求，实现前必须执行需求漏斗**（docs/ai/REQUIREMENT_GUIDE.md）：确认意图与决策点后方可实现；UI 需求必须确认页面类型与气质（营销型/信息型/工具型）及参考方向；禁止在"铺满""好看""大气"等含糊需求上直接动手；
8. **需求未经漏斗确认即动手 = 重大失误**（与删除数据库同级）；需求含糊词未澄清即实现，同属此列。
