# DESIGN.md（模板）

> 复制后按模块实际填充；公开 API 契约必须与代码一致。

## 职责

（本模块负责什么，明确不负责什么——非目标）

## 边界

（模块内允许/禁止做的操作；对外只暴露什么）

## 公开 API 契约（跨模块调用方按此使用）

| API/类 | 方法 | 契约 | 调用方 |
| --- | --- | --- | --- |

（禁止调用方直接访问本模块 Mapper/DO/内部 Service）

## 与其他模块的组合

（依赖哪些模块、被哪些模块依赖、组合时的注意事项）

## 表结构所有权

（本模块拥有的表；其他模块引用时只存标识、不建外键）
