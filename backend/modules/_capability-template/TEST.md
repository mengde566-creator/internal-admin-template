# TEST.md（模板）

> 复制后按模块实际填充；本清单是模块必须覆盖的关键用例与验收点。

## 必测用例

| # | 场景 | 预期 | 状态 |
| --- | --- | --- | --- |

## 安全/权限用例

（未登录 401、越权 403、参数错误 400、会话保持、关键操作审计）

## 数据完整性用例

（软删除后不可见不可登录、被引用实体删除拒绝、更新 null=不修改）

## 验收点

（与需求 REQ 对应的验收标准；质量门禁命令）
