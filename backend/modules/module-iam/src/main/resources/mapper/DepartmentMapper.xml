<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.internaladmin.module.iam.mapper.DepartmentMapper">
    <update id="compareAndIncrementRootVersion">
        UPDATE iam_department
        SET version = version + 1
        WHERE id = 1
          AND deleted = 0
          AND version = #{expectedVersion}
    </update>

    <select id="selectByCodeIncludingDeleted" resultType="com.internaladmin.module.iam.model.entity.DepartmentDO">
        SELECT id, code, name, parent_id, sort_order, enabled, deleted, version
        FROM iam_department
        WHERE code = #{code}
    </select>
</mapper>
