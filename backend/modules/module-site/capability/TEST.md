# module-site 测试清单

> 最后核对：2026-08-11。V01-08B 的真实 Spring + SQLite `SiteFlowTest` 已 4/4 通过；已自动覆盖的发布原子性、公开文件、撤回、草稿隔离与审计见下表，未覆盖项仍保持手动状态。

## 必测用例

| # | 场景 | 预期 | 状态 |
| --- | --- | --- | --- |
| 1 | 获取草稿（无草稿） | 返回 null（前端空表单） | ✅ 手动验证 |
| 2 | 保存草稿（含布局 GRID_SPLIT） | 内容+配色+布局+主图校验通过，200，返回含布局 | ✅ 自动（`SiteFlowTest`） |
| 3 | 保存草稿（非法配色） | 400 不支持的配色编码 | ✅ 手动验证 |
| 4 | 保存草稿（主图不存在） | 400 主展示图片不存在 | ✅ 手动验证 |
| 5 | 保存草稿（非法布局） | 400 不支持的布局编码 | ✅ 手动验证 |
| 6 | 保存草稿（非法区块类型） | 400 不支持的区块类型 | ✅ 手动验证 |
| 7 | 保存草稿（区块配图不存在） | 400 区块配图不存在 | ✅ 手动验证 |
| 8 | 区块整体保存 | 新增无 id 区块、更新有 id 区块、删除列表外旧区块、sortOrder 按数组顺序 | ✅ 手动验证 |
| 9 | 发布 | 快照生成 visible=true、复制布局与区块、审计 SITE_PUBLISH SUCCESS | ✅ 自动（`SiteFlowTest`） |
| 10 | 匿名读公开主页（含区块） | 返回已发布内容（布局+区块+双配色） | ✅ 自动（`SiteFlowTest`） |
| 11 | 匿名读公开图片（区块配图） | 仅可见快照或其区块引用的图片可读，未发布图片 404 | ✅ 自动（`SiteFlowTest`） |
| 12 | 匿名读公开主页（空区块） | 返回布局+空区块列表（前端兜底，不空白报错） | ✅ 手动验证 |
| 13 | 撤回 | visible=false、审计 SITE_WITHDRAW SUCCESS | ✅ 自动（`SiteFlowTest`） |
| 14 | 撤回后匿名读取 | 主页/图片均 404 | ✅ 自动（`SiteFlowTest`） |
| 15 | 草稿与快照隔离 | 修改草稿（布局/区块）不影响已发布快照 | ✅ 自动（`SiteFlowTest`） |
| 16 | 发布失败 | 旧快照保持有效（无半发布状态） | ✅ 自动（`SiteFlowTest`） |
| 17 | 无发布权限用户 | 发布/撤回 403 | ✅ 手动验证 |
| 18 | 无 edit 权限用户 | 草稿接口 403 | ✅ 手动验证 |

## 通用用例

安全/权限通用用例见工程顶层 `docs/development/CAPABILITY_COMMON.md` 第 3 节（401/403/CSRF 已覆盖）。

## 验收点

- 对应 REQ（公开主页改版）：布局渲染+铺满浏览器、区块增删改/排序/配图、草稿发布隔离、撤回、双配色、空区块兜底；
- 无数据库质量门禁：`./scripts/quality.sh --no-database` 已通过；
- 数据库完整层、迁移和剩余手动项由 V01-10B/V01-12 收口；区块整体保存的新增/更新/删除/排序完整组合仍不得描述为自动覆盖。
