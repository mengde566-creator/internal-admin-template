<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.internaladmin</groupId>
        <artifactId>internal-admin-template</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>module-site</artifactId>
    <name>module-site</name>
    <description>公开主页草稿、预览、发布、撤回和匿名读取</description>

    <dependencies>
        <dependency>
            <groupId>com.internaladmin</groupId>
            <artifactId>platform-kernel</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.internaladmin</groupId>
            <artifactId>platform-web</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.internaladmin</groupId>
            <artifactId>platform-data</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 跨模块：通过 module-file 公开 API 校验和使用展示图片 -->
        <dependency>
            <groupId>com.internaladmin</groupId>
            <artifactId>module-file</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 跨模块：发布/撤回结果通过 module-audit 公开 API 记录 -->
        <dependency>
            <groupId>com.internaladmin</groupId>
            <artifactId>module-audit</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
