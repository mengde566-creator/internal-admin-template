# 物品编码规则（合成测试资料，当前版）

本资料说明物品业务编码的管理边界，编码不是数据库内部标识。

## 全局唯一

每个启用或停用的物品都使用全局唯一的业务编码，不能把同一个编码分配给两个物品。

## 字符格式

编码只允许大写字母、数字和短横线。输入时应保留完整编码，不要截短或自行改写。

## 创建后的变更

物品创建后业务编码不可修改。需要新的编码时应创建新的受控业务对象。

## 名称更新

物品名称可以按受控业务流程更新，但名称更新不改变原有编码的唯一事实。

## 与内部标识的区别

业务编码用于查询和展示；数据库内部 ID 只由服务端保存和使用，不要求仓储人员提供。
