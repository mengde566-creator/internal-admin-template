# 仓库与库位编码规则（合成测试资料，当前版）

本资料说明仓储位置的业务编码规则，展示和查询均使用业务编码。

## 仓库编码

仓库编码在系统内全局唯一。两个不同仓库不能使用相同的业务编码。

## 库位编码

库位编码只要求在所属仓库内唯一。不同仓库可以各自拥有同名库位。

## 查询展示

用户查询位置时应看到仓库和库位业务名称或编码。服务端负责把业务对象映射到内部标识。

## 不需要内部 ID

仓储人员不需要知道或提交数据库内部 ID。页面和受控接口都不能把内部 ID 当作业务编码展示。
